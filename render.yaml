services:
  - type: web
    name: fontbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -u bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false 