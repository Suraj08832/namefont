services:
  - type: web
    name: fontbot
    runtime: python3
    pythonVersion: 3.9
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -u bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free
    healthCheckPath: /health
    autoDeploy: true 